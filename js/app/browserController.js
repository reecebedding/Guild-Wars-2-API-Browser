(function(angular){
	var module = angular.module("apiBrowser", []);

	module.controller("browserController", ["$scope", "$http", "$window", function($scope, $http, $window){
		
	}]);
})(window.angular);
